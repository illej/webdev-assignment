/*  JQUERY 
var elementPosition = $('#sticky-nav').offset();

$(window).scroll(function () {
    if ($(window).scrollTop() > elementPosition.top) {
        $('#sticky-nav').css('position', 'fixed').css('top', '0').css('width', '52em');

    } else {
        $('#sticky-nav').css('position', 'static');
    }
});*/